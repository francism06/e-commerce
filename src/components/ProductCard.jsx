import React from 'react';

export const ProductCard = ({ item }) => {
    console.log(item);

    return (
        <div className='p-2 flex flex-row w-full h-full bg-white border-2 border-black drop-shadow-primary'>
            ProductCard
        </div>
    )
}
